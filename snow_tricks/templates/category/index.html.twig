{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h1>Catégories</h1>

    {% if categories %}
        <p>Voici la liste des catégories de figures de SnowTricks. Sélectionnez celle que vous souhaitez modifier ou supprimer, ou <a href="{{ path('app_category_new') }}">créez-en une nouvelle</a> !</p>

        {% for category in categories %}
            <div class="row mb-3">
                <div class="col-3">
                    <h2 class="h4 fw-bold{% if category.isDefault %} text-muted {% endif %}">
                        {{ category.name|capitalize }}
                    </h2>
                </div>
                <div class="col-9">
                    <a class="btn btn-outline-primary btn-sm link-primary" href="{{ path('app_category_edit', {id: category.id}) }}">Modifier</a>
                    {% if not category.isDefault %}
                        {% include('category/_delete_form.html.twig') %}
                    {% endif %}
                </div>
            </div>
        {% endfor %}
    {% else %}
        <p>Aucune catégorie n'a été créé pour l'instant. <a href="{{ path('app_category_new') }}">Créez la première dès maintenant</a> !</p>
    {% endif %}
{% endblock %}
